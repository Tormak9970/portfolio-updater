import { Writable, writable } from "svelte/store";

export const projsList:Writable<any[]> = writable([]);
export const artList:Writable<any[]> = writable([]);
export const expList:Writable<any[]> = writable([]);
export const orgsList:Writable<any[]> = writable([]);
export const archList:Writable<any[]> = writable([]);